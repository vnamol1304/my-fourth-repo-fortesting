#Devops Niginx demo

This is to install nginx and demo

##steps

sudo apt-get install
sudo apt install nginx

sudo systemctl start nginx
sudo systemctl enable nginx

#checking for pull from remote to locatl
